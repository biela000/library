export type Magazine = {
  id: string,
  title: string,
  publisher: string,
  format: string,
  pageCount: number,
  coverUrl: string,
  fileUrl: string,
  scan: string,
  processing: string,
  sender: string,
}
